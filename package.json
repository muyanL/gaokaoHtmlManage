{
    "devDependencies": {
        "browser-sync": "^2.26.3"
    },
    "scripts": {
        "dev": "browser-sync start --server --files \"*.html,css/*.css,js/*.js\"",
        "start": "npm run dev"
    }
}